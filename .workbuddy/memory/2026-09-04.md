# 2026-09-04 工作日志

## 项目：纸页（PDF/图片转 Word，Flask + PyMuPDF Web 工具）

完成三项功能完善（走 software-company 团队 SOP：工程师寇豆码实现 → QA 严过关独立验证）：

1. **文件列表字典序排序**：`web/static/js/app.js` 的 `addFiles()` 按 `file.name` 用 `localeCompare`（自然数字排序 + 大小写不敏感）升序排列，排序后按 `fileKey` 重定位 selectedIndex。
2. **图片预览放大到 200%**：上限由 100% 放宽到 200%，同步改 `index.html`（range max=200）、`app.js`（validateSettings）、`converter.py`（LayoutConfig.validate ≤200；render_preview dpi 110→150 保清晰）、`app.css`（图片改显式 width/height:var(--image-scale)+object-fit:contain+flex-shrink:0，.word-content overflow visible）。
3. **打包单文件 exe**：新增 `build.spec`（PyInstaller onefile + datas web/templates、web/static）；`web/app.py` 增加 `_is_frozen()`/`_resource_root()`（冻结用 sys._MEIPASS + 临时目录数据目录）。产物 `dist/PdfToWord.exe`（≈52MB），已冒烟测试（curl / 返回 HTML、/api/convert image_scale=200 端到端成功）。

验证：单元测试 11/11 通过（新增 3 个 200% 缩放单测）。QA_RESULT: PASS。

关键环境备忘：venv 位于 `C:\Users\10174\.workbuddy\binaries\python\envs\default\Scripts\`（Windows 用 Scripts/ 而非 bin/），已装 Flask/PyMuPDF/Pillow/python-docx/docxcompose + PyInstaller。
